(function (window) {
  window.__env = window.__env || {};
  // Instructions: The deployment system should set this file's value.
  // Example: window.__env.API_BASE_URL = 'https://backend.example.com';
  // Default will fallback to '/api' if unset.
  // window.__env.API_BASE_URL = '';
})(this);
